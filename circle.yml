machine:
  timezone:
    Asia/Tokyo
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull minimum2scp/baseimage:latest

test:
  override:
    - docker run --name c1 -d minimum2scp/baseimage:latest
    - docker stop c1
    - docker rm c1

